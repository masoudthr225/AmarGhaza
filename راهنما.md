# 🍽️ برنامه آمار غذا — راهنمای کامل

## ▶️ اجرای برنامه

روی فایل **«اجرای برنامه.vbs»** دابل‌کلیک کنید. تمام.

برنامه در یک پنجره مستقل باز می‌شود — **بدون پنجره مشکی CMD و بدون نوار آدرس مرورگر**.

> اگر ویندوز پیام امنیتی داد، روی **More info** و سپس **Run anyway** بزنید.
> این پیام فقط به این دلیل است که فایل امضای دیجیتال تجاری ندارد.

### میان‌بر روی دسکتاپ
روی «اجرای برنامه.vbs» راست‌کلیک → **Send to** → **Desktop (create shortcut)**

سپس روی میان‌بر راست‌کلیک → **Properties** → دکمه **Change Icon** → می‌توانید آیکون دلخواه از پوشه `assets` انتخاب کنید.

---

## 📁 محتوای پوشه

| فایل / پوشه | توضیح |
|---|---|
| `اجرای برنامه.vbs` | ⭐ تنها فایلی که باید اجرا کنید |
| `راهنما.md` | همین راهنما |
| `index.html` | صفحه اصلی برنامه |
| `AppData` | 💾 اطلاعات شما (پرسنل، منو، آمار، تنظیمات) |
| `css/` `js/` `assets/` | فایل‌های برنامه — حذف نکنید |
| `desktop/` `tools/` | ابزارهای ساخت نسخه exe |

---

## 💾 محل ذخیره اطلاعات و پشتیبان‌گیری

همه اطلاعات در پوشه **`AppData`** کنار همین برنامه ذخیره می‌شود.
یعنی برنامه **کاملاً پرتابل** است — می‌توانید کل پوشه را روی فلش کپی کنید
و روی هر کامپیوتر دیگری با همان اطلاعات اجرا کنید.

### پشتیبان‌گیری
از دکمه **ساخت زیپ** داخل خود برنامه استفاده کنید.

### به‌روزرسانی به نسخه جدید
۱. از دکمه **ساخت زیپ** پشتیبان بگیرید
۲. پوشه `AppData` را از نسخه قدیمی کپی کنید
۳. در پوشه نسخه جدید جای‌گذاری (Paste) کنید

⚠️ پوشه `AppData` را هرگز پاک نکنید — همه اطلاعاتتان همان‌جاست.

---

## 🖨️ تنظیم پرینتر

در تب **⚙️ تنظیمات صفحه** اندازه کاغذ را انتخاب کنید:

| پرینتر | گزینه مناسب |
|---|---|
| پرینتر معمولی اداری | `A4` یا `A5` |
| **EPSON TM-T88IV** (حرارتی) | **`Roll Paper 80×297`** |
| پرینتر حرارتی باریک | `Roll 58mm` |

نکته برای TM-T88IV: عرض کاغذ ۸ سانتی‌متر است ولی فقط ۷٫۲ سانتی‌متر آن قابل چاپ است؛
برنامه به‌طور خودکار حاشیه راست و چپ را ۰٫۴ سانتی‌متر تنظیم می‌کند.

در همان تب می‌توانید حاشیه، فونت، عرض و چیدمان هر ستون، کادر، رنگ و سربرگ را کامل تنظیم کنید.

---

## 🪟 ساخت نسخه exe (اختیاری)

اگر می‌خواهید برنامه به‌صورت یک اپلیکیشن واقعی ویندوز با آیکون خودش نصب شود:

### گرفتن فایل آماده
به صفحه **Releases** مخزن بروید:
`https://github.com/masoudthr225/AmarGhaza/releases`

| فایل | توضیح |
|---|---|
| `AmarGhaza-Setup-*.exe` | نصب‌کننده — میان‌بر دسکتاپ و منوی استارت می‌سازد |
| `AmarGhaza-Portable-*.exe` | پرتابل — تک‌فایل، بدون نصب، روی فلش هم اجرا می‌شود |

### ساخت روی کامپیوتر خودتان
پیش‌نیاز: [Node.js](https://nodejs.org) نصب باشد.
روی `tools\build-windows.bat` دابل‌کلیک کنید. خروجی در `desktop\dist-win` ساخته می‌شود.

---

## ❓ رفع اشکال

| مشکل | راه‌حل |
|---|---|
| تغییرات جدید را نمی‌بینم | داخل برنامه `Ctrl` + `Shift` + `R` بزنید |
| فایل vbs باز نمی‌شود | راست‌کلیک → **Open with** → **Microsoft Windows Based Script Host** |
| اسامی روی رول حرارتی چاپ نمی‌شوند | در تنظیمات صفحه، کاغذ درست را انتخاب و اندازه قلم را کمی کم کنید |
| اطلاعاتم پاک شد | پوشه `AppData` را از پشتیبان برگردانید |
