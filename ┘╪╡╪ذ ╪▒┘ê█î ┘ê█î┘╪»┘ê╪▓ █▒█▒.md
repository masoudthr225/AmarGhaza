# 🪟 اجرای «برنامه آمار غذا» روی ویندوز ۱۱

سه روش دارید — از ساده به پیشرفته:

---

## روش ۱ — پرتابل، بدون هیچ نصبی ⭐ (پیشنهادی برای شروع)

1. کل پوشه برنامه را روی ویندوز کپی کنید (مثلاً در `C:\AmarGhaza`).
   فایل‌های لازم: `index.html` + پوشه‌های `css` ، `js` ، `assets` + فایل `اجرای برنامه - ویندوز.bat`
2. روی **«اجرای برنامه - ویندوز.bat»** دابل‌کلیک کنید.
3. برنامه در یک **پنجره مستقل** (بدون نوار آدرس مرورگر، مثل یک برنامه ویندوزی واقعی) باز می‌شود.

- از Edge داخلی ویندوز ۱۱ استفاده می‌کند — چیزی نصب نمی‌شود.
- اطلاعات برنامه در پوشه `AppData` کنار برنامه ذخیره می‌شود؛ یعنی **کاملاً پرتابل** است و می‌توانید کل پوشه را روی فلش هم بگذارید.
- برای میان‌بر دسکتاپ: روی فایل bat راست‌کلیک → Send to → Desktop (create shortcut)

---

## روش ۲ — ساخت فایل exe واقعی (نصب‌کننده + پرتابل)

پیش‌نیاز: [Node.js](https://nodejs.org) روی ویندوز نصب باشد.

1. پوشه برنامه را روی ویندوز کپی کنید.
2. روی **`tools\build-windows.bat`** دابل‌کلیک کنید.
3. بعد از پایان، در پوشه `desktop\dist-win` دو فایل ساخته می‌شود:
   - `AmarGhaza Setup 1.0.0.exe` → **نصب‌کننده**: نصب کنید، میان‌بر دسکتاپ و منوی استارت ساخته می‌شود
   - `AmarGhaza-Portable-1.0.0.exe` → **پرتابل**: تک‌فایل، بدون نصب اجرا می‌شود

نسخه exe با موتور Electron ساخته می‌شود: پنجره اختصاصی، آیکون اختصاصی، منوی فارسی و چاپ با `Ctrl+P`.

---

## روش ۳ — ساخت خودکار exe با GitHub (بدون نیاز به Node.js روی سیستم شما)

فایل workflow آماده است: `tools/github-workflow-build-windows.yml`

فعال‌سازی (فقط یک بار):
1. در مخزن گیت‌هاب، فایل بالا را به مسیر `.github/workflows/build-windows.yml` منتقل کنید
   (از طریق سایت گیت‌هاب: Add file → Create new file → مسیر را بنویسید و محتوا را کپی کنید).
2. از تب **Actions** گزینه «ساخت نسخه ویندوز (exe)» را اجرا کنید (Run workflow).
3. بعد از چند دقیقه، فایل‌های exe در بخش **Releases** مخزن قرار می‌گیرند و با هر تغییر کد، خودکار نسخه جدید ساخته می‌شود.

> ⚠️ به دلیل محدودیت دسترسی، این فایل باید توسط خود شما (صاحب مخزن) به پوشه workflows منتقل شود.

---

## ❓ سوالات متداول

**اطلاعات کجا ذخیره می‌شود؟**
- روش ۱: پوشه `AppData` کنار خود برنامه (پرتابل)
- روش ۲ و ۳: پروفایل کاربر ویندوز (`%APPDATA%`)
- در هر دو حالت با دکمه «💾 پشتیبان» می‌توانید فایل پشتیبان بگیرید و منتقل کنید.

**پرینتر حرارتی چطور وصل می‌شود؟**
درایور ویندوزی پرینتر را نصب کنید؛ موقع چاپ در پنجره چاپ، پرینتر حرارتی را انتخاب کنید. در تنظیمات صفحه برنامه هم «رول ۸۰mm» یا «رول ۵۸mm» را انتخاب کنید.

**به‌روزرسانی برنامه؟**
فقط فایل‌های تغییرکرده (طبق `js/manifest.js`) را جایگزین کنید — اطلاعات شما دست‌نخورده می‌ماند.
