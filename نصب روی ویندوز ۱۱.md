# 🪟 اجرای «برنامه آمار غذا» روی ویندوز

## ⭐ روش پیشنهادی — فایل exe مستقل (بدون CMD، بدون مرورگر، بدون نصب Node)

برنامه به‌صورت یک اپلیکیشن واقعی ویندوز ساخته می‌شود: پنجره اختصاصی، آیکون خودش،
و **هیچ پنجره مشکی CMD یا نوار آدرس مرورگری باز نمی‌شود**.

### گرفتن فایل exe آماده

1. به صفحه **Releases** مخزن گیت‌هاب بروید:
   `https://github.com/masoudthr225/AmarGhaza/releases`
2. از آخرین ریلیز یکی از این دو فایل را دانلود کنید:

| فایل | توضیح |
|---|---|
| **AmarGhaza-Setup-*.exe** | نصب‌کننده (پیشنهادی) — نصب می‌شود و میان‌بر دسکتاپ و منوی استارت می‌سازد |
| **AmarGhaza-Portable-*.exe** | پرتابل — تک‌فایل، بدون نصب؛ روی فلش هم اجرا می‌شود |

3. دابل‌کلیک کنید. تمام.

> ⚠️ هنگام اجرای اولین بار ممکن است ویندوز پیام **«Windows protected your PC»** بدهد
> (چون فایل امضای دیجیتال تجاری ندارد). روی **More info** و سپس **Run anyway** بزنید.

### ساخت exe به‌صورت خودکار

فایل workflow فعال است (`.github/workflows/build-windows.yml`). با هر تغییر در برنامه،
گیت‌هاب به‌طور خودکار نسخه ویندوز را می‌سازد. برای ساخت دستی:

**Actions** → **ساخت نسخه ویندوز (exe)** → **Run workflow**

بعد از چند دقیقه فایل‌ها هم در بخش **Artifacts** همان اجرا و هم در **Releases** قرار می‌گیرند.

---

## روش جایگزین — ساخت exe روی کامپیوتر خودتان

پیش‌نیاز: [Node.js](https://nodejs.org) نصب باشد.

روی **`tools\build-windows.bat`** دابل‌کلیک کنید. خروجی در `desktop\dist-win` ساخته می‌شود.

---

## روش ساده (بدون exe) — اجرا با مرورگر

اگر فقط می‌خواهید سریع برنامه را ببینید، روی **`Run-AmarGhaza.bat`** دابل‌کلیک کنید.
برنامه در پنجره مستقل Edge/Chrome باز می‌شود.

> این روش برنامه را در حالت پنجره‌ای باز می‌کند ولی همچنان به مرورگر وابسته است.
> برای استفاده روزمره، نسخه exe بالا را ترجیح دهید.

---

## 💾 محل ذخیره اطلاعات

اطلاعات (پرسنل، منو، آمار روز، تنظیمات چاپ) روی همان سیستم ذخیره می‌شود.
برای پشتیبان‌گیری از دکمه **ساخت زیپ** داخل خود برنامه استفاده کنید.
